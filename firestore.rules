rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read/write access for game sessions and tables
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
